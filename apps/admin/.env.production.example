# ============================================================
# Admin dashboard — Production environment template
# ============================================================
# Set in Vercel's project Environment Variables (Production scope).
# NEXT_PUBLIC_* values are baked into the build at deploy time.

NEXT_PUBLIC_API_URL=https://pizza-height-api.up.railway.app/api/v1
NEXT_PUBLIC_WS_URL=https://pizza-height-api.up.railway.app
